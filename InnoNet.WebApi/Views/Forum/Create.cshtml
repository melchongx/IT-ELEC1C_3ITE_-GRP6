@model InnoNet.WebApi.Models.Forum.AddForumModel
@{
    ViewData["Title"] = "Create";
}

<div class="container body-content">
    <div class="row sectionHeader">
        <div class="sectionHeading">
            Create a new category on the forum
        </div>
    </div>
    <div class="row frontPageContent">
        <div id="createForumSection">
            <form asp-action="CreateForum" method="post" id="createForumForm" enctype="multipart/form-data">
                <div asp-validation-summary="All" class="text-danger"> </div>
                <div class="form-group">
                    <label asp-for="Title" class="text-light">Category</label>
                    <input asp-for="Title" class="form-control" required placeholder="Choose category title" maxlength="100" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Description" class="text-light">Description</label>
                    <input asp-for="Description" class="form-control" required placeholder="Choose category description" maxlength="300" />
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <div id="upload">
                    <label class="text-light">Upload category image</label><br />
                    <label class="btn btn-default btn-file">
                        Choose <input asp-for="ImageUpload" type="file" accept="image/*" style="display:none" />
                    </label>
                </div>
                <button type="submit" class="btn btn-submitPost">Create</button>
            </form>
        </div>
    </div>
</div>
